# import sqlite3

# connection = sqlite3.connect('femo.db')

# cursor = connection.cursor()

# cursor.execute("CREATE TABLE user(id INTEGER NOT NULL PRIMARY KEY, username TEXT varchar(12));")


# connection.close()